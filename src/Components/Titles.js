import React from "react";

const Titles = () => (
    <div>
        <h1 className="title-container__title">Weather Finder</h1>
        <h3 className="title-container__subtitle">I’m using a free version of openweathermap.org to fetch the API. So, the only city and country that will load is London, GB.</h3>
    </div>   
);

export default Titles;
